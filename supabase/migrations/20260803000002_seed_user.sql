-- Criação do usuário técnico padrão.
-- Rode este SQL no SQL Editor do Supabase após ter configurado o Auth (por exemplo, após o primeiro deploy).
-- Substitua a senha por uma senha forte.

-- Remova o comentário da linha abaixo e execute:
-- SELECT auth.create_user(
--   jsonb_build_object(
--     'email', 'admin@rdsolutions.com.br',
--     'password', 'SUA_SENHA_FORTE_AQUI',
--     'email_confirm', true,
--     'role', 'authenticated'
--   )
-- );

-- Para enviar convite por email em vez de definir senha fixa, use o painel do Supabase:
-- Authentication > Users > Add user > Send email invite.
